import {ConvertNumberToPositionPipe} from './convert-number-to-position.pipe';

describe('ConvertNumberToPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertNumberToPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
